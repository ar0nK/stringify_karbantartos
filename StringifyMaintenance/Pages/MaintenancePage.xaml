<Page x:Class="StringifyMaintenance.Pages.MaintenancePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      Background="#F5F6FA"
      MinHeight="780"
      MinWidth="1100">

    <Page.Resources>

        <Style x:Key="FormInput" TargetType="TextBox">
            <Setter Property="Height" Value="34" />
            <Setter Property="Padding" Value="10,0" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Foreground" Value="#111827" />
            <Setter Property="Background" Value="#F9FAFB" />
            <Setter Property="BorderBrush" Value="#D1D5DB" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Margin="{TemplateBinding Padding}"
                                          VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#2563EB" />
                            </Trigger>
                            <Trigger Property="IsReadOnly" Value="True">
                                <Setter Property="Background" Value="#F3F4F6" />
                                <Setter Property="Foreground" Value="#9CA3AF" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="FormInputMulti" TargetType="TextBox" BasedOn="{StaticResource FormInput}">
            <Setter Property="Height" Value="72" />
            <Setter Property="VerticalContentAlignment" Value="Top" />
            <Setter Property="Padding" Value="10,8" />
        </Style>

        <Style x:Key="FormPassword" TargetType="PasswordBox">
            <Setter Property="Height" Value="34" />
            <Setter Property="Padding" Value="10,0" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Foreground" Value="#111827" />
            <Setter Property="Background" Value="#F9FAFB" />
            <Setter Property="BorderBrush" Value="#D1D5DB" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Margin="{TemplateBinding Padding}"
                                          VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#2563EB" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="DatePicker">
            <Setter Property="Height" Value="34" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Foreground" Value="#111827" />
            <Setter Property="Background" Value="#F9FAFB" />
            <Setter Property="BorderBrush" Value="#D1D5DB" />
            <Setter Property="BorderThickness" Value="1" />
        </Style>

        <Style TargetType="CheckBox">
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Foreground" Value="#111827" />
            <Setter Property="FontSize" Value="13" />
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Height" Value="32" />
            <Setter Property="Padding" Value="18,0" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Background" Value="#2563EB" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#1D4ED8" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#1E40AF" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Background" Value="#EF4444" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#DC2626" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#B91C1C" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="GhostButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Background" Value="#F3F4F6" />
            <Setter Property="Foreground" Value="#374151" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#E5E7EB" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#D1D5DB" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="FieldLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Foreground" Value="#374151" />
            <Setter Property="FontWeight" Value="Medium" />
            <Setter Property="Margin" Value="0,0,0,4" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>

        <Style TargetType="GroupBox">
            <Setter Property="BorderBrush" Value="#E5E7EB" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="16" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Foreground" Value="#6B7280" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="GroupBox">
                        <Border Background="White"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="*" />
                                </Grid.RowDefinitions>
                                <Border Grid.Row="0"
                                        Background="#F9FAFB"
                                        BorderBrush="#E5E7EB"
                                        BorderThickness="0,0,0,1"
                                        Padding="16,10"
                                        CornerRadius="10,10,0,0">
                                    <ContentPresenter ContentSource="Header"
                                                      TextBlock.FontWeight="SemiBold"
                                                      TextBlock.FontSize="13"
                                                      TextBlock.Foreground="#111827" />
                                </Border>
                                <ContentPresenter Grid.Row="1" Margin="{TemplateBinding Padding}" />
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.45" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TabControl">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="0" />
        </Style>

        <Style TargetType="TabItem">
            <Setter Property="FontSize" Value="13" />
            <Setter Property="FontWeight" Value="Medium" />
            <Setter Property="Foreground" Value="#6B7280" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="16,8" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="Transparent"
                                BorderThickness="0,0,0,2"
                                Padding="{TemplateBinding Padding}"
                                Margin="0,0,4,0">
                            <ContentPresenter ContentSource="Header" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="#2563EB" />
                                <Setter Property="Foreground" Value="#2563EB" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="#111827" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="White" />
            <Setter Property="BorderBrush" Value="#E5E7EB" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="RowBackground" Value="White" />
            <Setter Property="AlternatingRowBackground" Value="#F9FAFB" />
            <Setter Property="HorizontalGridLinesBrush" Value="#F3F4F6" />
            <Setter Property="VerticalGridLinesBrush" Value="Transparent" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Foreground" Value="#374151" />
            <Setter Property="ColumnHeaderHeight" Value="40" />
            <Setter Property="RowHeight" Value="38" />
            <Setter Property="SelectionMode" Value="Single" />
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#F9FAFB" />
            <Setter Property="Foreground" Value="#6B7280" />
            <Setter Property="FontSize" Value="11" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Padding" Value="12,0" />
            <Setter Property="BorderBrush" Value="#E5E7EB" />
            <Setter Property="BorderThickness" Value="0,0,0,1" />
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="Padding" Value="12,0" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Padding="{TemplateBinding Padding}"
                                Background="{TemplateBinding Background}">
                            <ContentPresenter VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#EFF6FF" />
                    <Setter Property="Foreground" Value="#1D4ED8" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="DataGridRow">
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#EFF6FF" />
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F0F9FF" />
                </Trigger>
            </Style.Triggers>
        </Style>

    </Page.Resources>

    <Grid Margin="28,0,28,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Padding="0,20,0,18">
            <DockPanel>
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Left" VerticalAlignment="Center">
                    <Border Width="36" Height="36" Background="#2563EB" CornerRadius="8" Margin="0,0,12,0">
                        <TextBlock Text="S" FontSize="18" FontWeight="Bold" Foreground="White"
                                   HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <StackPanel VerticalAlignment="Center">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Stringify" FontSize="18" FontWeight="Bold" Foreground="#111827" />
                            <TextBlock Text=" Maintenance" FontSize="18" FontWeight="Bold" Foreground="#2563EB" />
                        </StackPanel>
                        <TextBlock Text="Admin panel" FontSize="11" Foreground="#9CA3AF" Margin="0,1,0,0" />
                    </StackPanel>
                </StackPanel>

                <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" VerticalAlignment="Center" HorizontalAlignment="Right">
                    <Border Background="#F3F4F6" CornerRadius="20" Padding="10,6" Margin="0,0,12,0">
                        <StackPanel Orientation="Horizontal">
                            <Border Width="22" Height="22" Background="#2563EB" CornerRadius="11" Margin="0,0,7,0">
                                <TextBlock Text="{Binding CurrentUserInitial}" FontSize="11" FontWeight="SemiBold"
                                           Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Border>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="Signed in as" Foreground="#9CA3AF" FontSize="10" LineHeight="13" />
                                <TextBlock Text="{Binding CurrentUserName}" Foreground="#111827" FontSize="12" FontWeight="SemiBold" LineHeight="15" />
                            </StackPanel>
                        </StackPanel>
                    </Border>
                    <Button Content="Logout" Style="{StaticResource GhostButton}" Height="34" Padding="16,0" Click="LogoutButton_Click" />
                </StackPanel>
            </DockPanel>
        </Border>

        <Border Grid.Row="1" Height="1" Background="#E5E7EB" Margin="0,0,0,20" />

        <TabControl Grid.Row="2">

            <TabItem Header="Products">
                <Grid Margin="0,16,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <DataGrid ItemsSource="{Binding Products}"
                              SelectedItem="{Binding SelectedProduct}"
                              AutoGenerateColumns="False"
                              IsReadOnly="True"
                              Grid.Row="0"
                              Margin="0,0,0,16">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID"        Binding="{Binding Id}"           Width="60" />
                            <DataGridTextColumn Header="NAME"      Binding="{Binding Nev}"          Width="220" />
                            <DataGridTextColumn Header="PRICE"     Binding="{Binding Ar}"           Width="80" />
                            <DataGridCheckBoxColumn Header="AVAIL" Binding="{Binding Elerheto}"     Width="60" />
                            <DataGridTextColumn Header="TYPE ID"   Binding="{Binding GitarTipusId}" Width="80" />
                            <DataGridTextColumn Header="CREATED"   Binding="{Binding Letrehozva}"   Width="160" />
                        </DataGrid.Columns>
                    </DataGrid>

                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="16" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <GroupBox Header="Selected product" Grid.Column="0" IsEnabled="{Binding HasSelectedProduct}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="90" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>

                                <TextBlock Text="Id"          Grid.Row="0" Grid.Column="0" Style="{StaticResource FieldLabel}" Margin="0,0,8,10" />
                                <TextBox   Text="{Binding SelectedProduct.Id}"                                                  Grid.Row="0" Grid.Column="1" Style="{StaticResource FormInput}"      IsReadOnly="True" Margin="0,0,0,10" />

                                <TextBlock Text="Name"        Grid.Row="1" Grid.Column="0" Style="{StaticResource FieldLabel}" Margin="0,0,8,10" />
                                <TextBox   Text="{Binding SelectedProduct.Nev,         UpdateSourceTrigger=LostFocus}"          Grid.Row="1" Grid.Column="1" Style="{StaticResource FormInput}"      Margin="0,0,0,10" />

                                <TextBlock Text="Short"       Grid.Row="2" Grid.Column="0" Style="{StaticResource FieldLabel}" Margin="0,0,8,10" />
                                <TextBox   Text="{Binding SelectedProduct.RovidLeiras, UpdateSourceTrigger=LostFocus}"          Grid.Row="2" Grid.Column="1" Style="{StaticResource FormInput}"      Margin="0,0,0,10" />

                                <TextBlock Text="Description" Grid.Row="3" Grid.Column="0" Style="{StaticResource FieldLabel}" Margin="0,0,8,10" />
                                <TextBox   Text="{Binding SelectedProduct.Leiras,      UpdateSourceTrigger=LostFocus}"          Grid.Row="3" Grid.Column="1" Style="{StaticResource FormInputMulti}" Margin="0,0,0,10" TextWrapping="Wrap" AcceptsReturn="True" />

                                <TextBlock Text="Price"       Grid.Row="4" Grid.Column="0" Style="{StaticResource FieldLabel}" Margin="0,0,8,10" />
                                <TextBox   Text="{Binding SelectedProduct.Ar,          UpdateSourceTrigger=LostFocus}"          Grid.Row="4" Grid.Column="1" Style="{StaticResource FormInput}"      Margin="0,0,0,10" />

                                <TextBlock Text="Available"   Grid.Row="5" Grid.Column="0" Style="{StaticResource FieldLabel}" Margin="0,0,8,10" />
                                <CheckBox  IsChecked="{Binding SelectedProduct.Elerheto}"                                       Grid.Row="5" Grid.Column="1" Margin="0,0,0,10" />

                                <TextBlock Text="Type Id"     Grid.Row="6" Grid.Column="0" Style="{StaticResource FieldLabel}" Margin="0,0,8,10" />
                                <TextBox   Text="{Binding SelectedProduct.GitarTipusId,UpdateSourceTrigger=LostFocus}"          Grid.Row="6" Grid.Column="1" Style="{StaticResource FormInput}"      Margin="0,0,0,10" />

                                <TextBlock Text="Created"     Grid.Row="7" Grid.Column="0" Style="{StaticResource FieldLabel}" Margin="0,0,8,10" />
                                <DatePicker SelectedDate="{Binding SelectedProduct.Letrehozva}"                                 Grid.Row="7" Grid.Column="1" Margin="0,0,0,10" />

                                <StackPanel Grid.Row="8" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
                                    <Button Content="Update" Style="{StaticResource PrimaryButton}" Width="88" Margin="0,0,8,0" Click="UpdateProduct_Click" />
                                    <Button Content="Delete" Style="{StaticResource DangerButton}"  Width="88" Click="DeleteProduct_Click" />
                                </StackPanel>
                            </Grid>
                        </GroupBox>

                        <GroupBox Header="New product" Grid.Column="2">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="90" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>

                                <TextBlock Text="Name"        Grid.Row="0" Grid.Column="0" Style="{StaticResource FieldLabel}" Margin="0,0,8,10" />
                                <TextBox   Text="{Binding NewProduct.Nev,         UpdateSourceTrigger=LostFocus}"               Grid.Row="0" Grid.Column="1" Style="{StaticResource FormInput}"      Margin="0,0,0,10" />

                                <TextBlock Text="Short"       Grid.Row="1" Grid.Column="0" Style="{StaticResource FieldLabel}" Margin="0,0,8,10" />
                                <TextBox   Text="{Binding NewProduct.RovidLeiras, UpdateSourceTrigger=LostFocus}"               Grid.Row="1" Grid.Column="1" Style="{StaticResource FormInput}"      Margin="0,0,0,10" />

                                <TextBlock Text="Description" Grid.Row="2" Grid.Column="0" Style="{StaticResource FieldLabel}" Margin="0,0,8,10" />
                                <TextBox   Text="{Binding NewProduct.Leiras,      UpdateSourceTrigger=LostFocus}"               Grid.Row="2" Grid.Column="1" Style="{StaticResource FormInputMulti}" Margin="0,0,0,10" TextWrapping="Wrap" AcceptsReturn="True" />

                                <TextBlock Text="Price"       Grid.Row="3" Grid.Column="0" Style="{StaticResource FieldLabel}" Margin="0,0,8,10" />
                                <TextBox   Text="{Binding NewProduct.Ar,          UpdateSourceTrigger=LostFocus}"               Grid.Row="3" Grid.Column="1" Style="{StaticResource FormInput}"      Margin="0,0,0,10" />

                                <TextBlock Text="Available"   Grid.Row="4" Grid.Column="0" Style="{StaticResource FieldLabel}" Margin="0,0,8,10" />
                                <CheckBox  IsChecked="{Binding NewProduct.Elerheto}"                                            Grid.Row="4" Grid.Column="1" Margin="0,0,0,10" />

                                <TextBlock Text="Type Id"     Grid.Row="5" Grid.Column="0" Style="{StaticResource FieldLabel}" Margin="0,0,8,10" />
                                <TextBox   Text="{Binding NewProduct.GitarTipusId,UpdateSourceTrigger=LostFocus}"               Grid.Row="5" Grid.Column="1" Style="{StaticResource FormInput}"      Margin="0,0,0,10" />

                                <TextBlock Text="Created"     Grid.Row="6" Grid.Column="0" Style="{StaticResource FieldLabel}" Margin="0,0,8,10" />
                                <DatePicker SelectedDate="{Binding NewProduct.Letrehozva}"                                      Grid.Row="6" Grid.Column="1" Margin="0,0,0,10" />

                                <StackPanel Grid.Row="7" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
                                    <Button Content="Add"   Style="{StaticResource PrimaryButton}" Width="88" Margin="0,0,8,0" Click="AddProduct_Click" />
                                    <Button Content="Clear" Style="{StaticResource GhostButton}"   Width="88" Click="ClearNewProduct_Click" />
                                </StackPanel>
                            </Grid>
                        </GroupBox>
                    </Grid>
                </Grid>
            </TabItem>

            <TabItem Header="Users">
                <Grid Margin="0,16,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <DataGrid ItemsSource="{Binding Users}"
                              SelectedItem="{Binding SelectedUser}"
                              AutoGenerateColumns="False"
                              IsReadOnly="True"
                              Grid.Row="0"
                              Margin="0,0,0,16">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID"         Binding="{Binding Id}"          Width="60" />
                            <DataGridTextColumn Header="NAME"       Binding="{Binding Nev}"         Width="180" />
                            <DataGridTextColumn Header="EMAIL"      Binding="{Binding Email}"       Width="240" />
                            <DataGridTextColumn Header="PERMISSION" Binding="{Binding Jogosultsag}" Width="100" />
                            <DataGridTextColumn Header="ACTIVE"     Binding="{Binding Aktiv}"       Width="80" />
                        </DataGrid.Columns>
                    </DataGrid>

                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="16" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <GroupBox Header="Selected user" Grid.Column="0" IsEnabled="{Binding HasSelectedUser}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="90" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>

                                <TextBlock Text="Id"           Grid.Row="0" Grid.Column="0" Style="{StaticResource FieldLabel}" Margin="0,0,8,10" />
                                <TextBox   Text="{Binding SelectedUser.Id}"                                       Grid.Row="0" Grid.Column="1" Style="{StaticResource FormInput}" IsReadOnly="True" Margin="0,0,0,10" />

                                <TextBlock Text="Name"         Grid.Row="1" Grid.Column="0" Style="{StaticResource FieldLabel}" Margin="0,0,8,10" />
                                <TextBox   Text="{Binding SelectedUser.Nev,         UpdateSourceTrigger=LostFocus}" Grid.Row="1" Grid.Column="1" Style="{StaticResource FormInput}" Margin="0,0,0,10" />

                                <TextBlock Text="Email"        Grid.Row="2" Grid.Column="0" Style="{StaticResource FieldLabel}" Margin="0,0,8,10" />
                                <TextBox   Text="{Binding SelectedUser.Email,       UpdateSourceTrigger=LostFocus}" Grid.Row="2" Grid.Column="1" Style="{StaticResource FormInput}" Margin="0,0,0,10" />

                                <TextBlock Text="Permission"   Grid.Row="3" Grid.Column="0" Style="{StaticResource FieldLabel}" Margin="0,0,8,10" />
                                <TextBox   Text="{Binding SelectedUser.Jogosultsag, UpdateSourceTrigger=LostFocus}" Grid.Row="3" Grid.Column="1" Style="{StaticResource FormInput}" Margin="0,0,0,10" />

                                <TextBlock Text="Active"       Grid.Row="4" Grid.Column="0" Style="{StaticResource FieldLabel}" Margin="0,0,8,10" />
                                <TextBox   Text="{Binding SelectedUser.Aktiv,       UpdateSourceTrigger=LostFocus}" Grid.Row="4" Grid.Column="1" Style="{StaticResource FormInput}" Margin="0,0,0,10" />

                                <TextBlock Text="New password" Grid.Row="5" Grid.Column="0" Style="{StaticResource FieldLabel}" Margin="0,0,8,10" />
                                <PasswordBox x:Name="UpdateUserPasswordBox"                                         Grid.Row="5" Grid.Column="1" Style="{StaticResource FormPassword}" Margin="0,0,0,10" />

                                <StackPanel Grid.Row="6" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
                                    <Button Content="Update" Style="{StaticResource PrimaryButton}" Width="88" Margin="0,0,8,0" Click="UpdateUser_Click" />
                                    <Button Content="Delete" Style="{StaticResource DangerButton}"  Width="88" Click="DeleteUser_Click" />
                                </StackPanel>
                            </Grid>
                        </GroupBox>

                        <GroupBox Header="New user" Grid.Column="2">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="90" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>

                                <TextBlock Text="Name"       Grid.Row="0" Grid.Column="0" Style="{StaticResource FieldLabel}" Margin="0,0,8,10" />
                                <TextBox   Text="{Binding NewUser.Nev,         UpdateSourceTrigger=LostFocus}" Grid.Row="0" Grid.Column="1" Style="{StaticResource FormInput}" Margin="0,0,0,10" />

                                <TextBlock Text="Email"      Grid.Row="1" Grid.Column="0" Style="{StaticResource FieldLabel}" Margin="0,0,8,10" />
                                <TextBox   Text="{Binding NewUser.Email,       UpdateSourceTrigger=LostFocus}" Grid.Row="1" Grid.Column="1" Style="{StaticResource FormInput}" Margin="0,0,0,10" />

                                <TextBlock Text="Permission" Grid.Row="2" Grid.Column="0" Style="{StaticResource FieldLabel}" Margin="0,0,8,10" />
                                <TextBox   Text="{Binding NewUser.Jogosultsag, UpdateSourceTrigger=LostFocus}" Grid.Row="2" Grid.Column="1" Style="{StaticResource FormInput}" Margin="0,0,0,10" />

                                <TextBlock Text="Active"     Grid.Row="3" Grid.Column="0" Style="{StaticResource FieldLabel}" Margin="0,0,8,10" />
                                <TextBox   Text="{Binding NewUser.Aktiv,       UpdateSourceTrigger=LostFocus}" Grid.Row="3" Grid.Column="1" Style="{StaticResource FormInput}" Margin="0,0,0,10" />

                                <TextBlock Text="Password"   Grid.Row="4" Grid.Column="0" Style="{StaticResource FieldLabel}" Margin="0,0,8,10" />
                                <PasswordBox x:Name="NewUserPasswordBox"                                       Grid.Row="4" Grid.Column="1" Style="{StaticResource FormPassword}" Margin="0,0,0,10" />

                                <StackPanel Grid.Row="5" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
                                    <Button Content="Add"   Style="{StaticResource PrimaryButton}" Width="88" Margin="0,0,8,0" Click="AddUser_Click" />
                                    <Button Content="Clear" Style="{StaticResource GhostButton}"   Width="88" Click="ClearNewUser_Click" />
                                </StackPanel>
                            </Grid>
                        </GroupBox>
                    </Grid>
                </Grid>
            </TabItem>

            <TabItem Header="Orders">
                <Grid Margin="0,16,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <DataGrid ItemsSource="{Binding Orders}"
                              SelectedItem="{Binding SelectedOrder}"
                              AutoGenerateColumns="False"
                              IsReadOnly="True"
                              Grid.Row="0"
                              Margin="0,0,0,16">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID"      Binding="{Binding Id}"            Width="60" />
                            <DataGridTextColumn Header="USER ID" Binding="{Binding FelhasznaloId}" Width="90" />
                            <DataGridTextColumn Header="TOTAL"   Binding="{Binding Osszeg}"        Width="90" />
                            <DataGridTextColumn Header="STATUS"  Binding="{Binding Status}"        Width="160" />
                            <DataGridTextColumn Header="DATE"    Binding="{Binding Datum}"         Width="160" />
                        </DataGrid.Columns>
                    </DataGrid>

                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="16" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <GroupBox Header="Selected order" Grid.Column="0" IsEnabled="{Binding HasSelectedOrder}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="90" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>

                                <TextBlock Text="Id"      Grid.Row="0" Grid.Column="0" Style="{StaticResource FieldLabel}" Margin="0,0,8,10" />
                                <TextBox   Text="{Binding SelectedOrder.Id}"                                       Grid.Row="0" Grid.Column="1" Style="{StaticResource FormInput}" IsReadOnly="True" Margin="0,0,0,10" />

                                <TextBlock Text="User Id" Grid.Row="1" Grid.Column="0" Style="{StaticResource FieldLabel}" Margin="0,0,8,10" />
                                <TextBox   Text="{Binding SelectedOrder.FelhasznaloId, UpdateSourceTrigger=LostFocus}" Grid.Row="1" Grid.Column="1" Style="{StaticResource FormInput}" Margin="0,0,0,10" />

                                <TextBlock Text="Total"   Grid.Row="2" Grid.Column="0" Style="{StaticResource FieldLabel}" Margin="0,0,8,10" />
                                <TextBox   Text="{Binding SelectedOrder.Osszeg,        UpdateSourceTrigger=LostFocus}" Grid.Row="2" Grid.Column="1" Style="{StaticResource FormInput}" Margin="0,0,0,10" />

                                <TextBlock Text="Status"  Grid.Row="3" Grid.Column="0" Style="{StaticResource FieldLabel}" Margin="0,0,8,10" />
                                <TextBox   Text="{Binding SelectedOrder.Status,        UpdateSourceTrigger=LostFocus}" Grid.Row="3" Grid.Column="1" Style="{StaticResource FormInput}" Margin="0,0,0,10" />

                                <TextBlock Text="Date"    Grid.Row="4" Grid.Column="0" Style="{StaticResource FieldLabel}" Margin="0,0,8,10" />
                                <DatePicker SelectedDate="{Binding SelectedOrder.Datum}"                               Grid.Row="4" Grid.Column="1" Margin="0,0,0,10" />

                                <StackPanel Grid.Row="5" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
                                    <Button Content="Update" Style="{StaticResource PrimaryButton}" Width="88" Margin="0,0,8,0" Click="UpdateOrder_Click" />
                                    <Button Content="Delete" Style="{StaticResource DangerButton}"  Width="88" Click="DeleteOrder_Click" />
                                </StackPanel>
                            </Grid>
                        </GroupBox>

                        <GroupBox Header="New order" Grid.Column="2">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="90" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>

                                <TextBlock Text="User Id" Grid.Row="0" Grid.Column="0" Style="{StaticResource FieldLabel}" Margin="0,0,8,10" />
                                <TextBox   Text="{Binding NewOrder.FelhasznaloId, UpdateSourceTrigger=LostFocus}" Grid.Row="0" Grid.Column="1" Style="{StaticResource FormInput}" Margin="0,0,0,10" />

                                <TextBlock Text="Total"   Grid.Row="1" Grid.Column="0" Style="{StaticResource FieldLabel}" Margin="0,0,8,10" />
                                <TextBox   Text="{Binding NewOrder.Osszeg,        UpdateSourceTrigger=LostFocus}" Grid.Row="1" Grid.Column="1" Style="{StaticResource FormInput}" Margin="0,0,0,10" />

                                <TextBlock Text="Status"  Grid.Row="2" Grid.Column="0" Style="{StaticResource FieldLabel}" Margin="0,0,8,10" />
                                <TextBox   Text="{Binding NewOrder.Status,        UpdateSourceTrigger=LostFocus}" Grid.Row="2" Grid.Column="1" Style="{StaticResource FormInput}" Margin="0,0,0,10" />

                                <TextBlock Text="Date"    Grid.Row="3" Grid.Column="0" Style="{StaticResource FieldLabel}" Margin="0,0,8,10" />
                                <DatePicker SelectedDate="{Binding NewOrder.Datum}"                                Grid.Row="3" Grid.Column="1" Margin="0,0,0,10" />

                                <StackPanel Grid.Row="4" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
                                    <Button Content="Add"   Style="{StaticResource PrimaryButton}" Width="88" Margin="0,0,8,0" Click="AddOrder_Click" />
                                    <Button Content="Clear" Style="{StaticResource GhostButton}"   Width="88" Click="ClearNewOrder_Click" />
                                </StackPanel>
                            </Grid>
                        </GroupBox>
                    </Grid>
                </Grid>
            </TabItem>

        </TabControl>
    </Grid>
</Page>